BiLSTM
BiLSTM_v1 -> BiLstm code to run on resnet101 csv file
BiLSTM_v2 -> BiLstm code to run on vldsp csv file
BiLSTM_v2 -> BiLstm code to run on combined csv file

feature_extraction
CNN_Features -> code file to extact resnet101 features
vldsp -> code file to extract VLDSP features

features_csv file
concatenated_file -> csv file of combination of resnet and vldsp features
Normalized_VLDSPFeatures2 -> csv file of normalised vldsp features 
ResNet101_features_LSTM -> csv file of resnet101 features
VLDSPFeatures2 -> csv file of vldsp features

graphs
graph, graph1 -> code files to generate graphs
best_bilstm_results -> bar graph with the best result of each algorithm
bilstm_all_layers -> bar graphs of best result with 2 and 3 layers
other_models -> bar garph with comparison of our method with previous works

concat -> code to combine to csv files
label1 -> conatin all the labels for all the videos
SecKeyFrames -> code file to extract all the key frames